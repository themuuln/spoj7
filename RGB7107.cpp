#include <iostream>
using namespace std;

int main() {
  int a, b, c, sum = 0;
  cin >> a >> b >> c;
  if (a % 2 == 0) {
    sum += a
  }
  if (b % 2 == 0) {
    sum += b
  }
  if (c % 2 == 0) {
    sum += c
  }
  cout << sum << endl;
}
